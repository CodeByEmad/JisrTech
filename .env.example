# The project's only secret (docs/engineering-blueprint.md §13).
# Used by the contact form server action from M3 onward. Set in Vercel
# project settings for deployments; in .env.local for local development.
RESEND_API_KEY=
